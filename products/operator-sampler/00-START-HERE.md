# START HERE — The Operator Sampler (free)

This is the free one — and it actually works. No stubs, no locked previews:
one real skill, one real script, one real daily rhythm, pulled straight from
the paid line so you can judge the whole shelf by it.

## Your first win in 3 steps (~10 minutes)

1. **Install the skill:** copy `install/claude-code/verify-before-claiming.md`
   into your Claude Code skills/commands folder (Codex:
   `install/codex/verify-before-claiming/SKILL.md` → `.agents/skills/`).
2. **Watch it change behavior:** ask your agent to fix any small bug. From now
   on, a completion claim without a fresh verification run in the same message
   doesn't count — and your agent knows it.
3. **Start your next session with the script:** open
   `payload/inspect-before-edit.md`, paste the prompt, fill the three blanks.
   The agent maps, narrates, plans, and names its proof before touching a file.

Then pin `payload/daily-operator-checklist.md` next to your terminal and run
the column that matches your day (15/30/60 minutes).

## What's in the folder

- `payload/verify-before-claiming.md` — the claim gate: evidence before
  assertions, never trust a sub-agent's success report. The same canonical
  file that ships in two paid packs.
- `payload/inspect-before-edit.md` — the one prompt script that prevents most
  AI-assisted disasters, with the reasoning behind each line.
- `payload/daily-operator-checklist.md` — the one-page daily rhythm: session
  tracks, verification gates, the bad-output protocol, the weekly review.
- `install/` — both agent layouts, ready to copy.

## Where this comes from

These aren't samples written for a giveaway — they're working files from the
paid ladder at aiwithmurda.com/store: The Future Proof Method ($47 starter
system), the New Wave Operator Bundle ($97, adds the safety + verification
packs), and The Operator Toolkit ($297, the full system). If the free tier
works this well, that's the point — judge the ladder by it.

MIT licensed. Share it whole if you like — attribution appreciated.
