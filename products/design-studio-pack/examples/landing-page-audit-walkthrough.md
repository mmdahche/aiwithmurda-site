# Walkthrough — Landing page audit

1. Generate or open a one-page marketing screen
2. Confirm `DESIGN.md` exists — if not, run `design-contract` first
3. Run `anti-slop-audit` — fix blocking failures
4. Run `ui-critique` on mobile + desktop captures
5. If motion exists, verify against `motion-framework` tiers

Ship only when critique verdict is APPROVE and anti-slop says SHIP.
